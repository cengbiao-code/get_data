# 本地上市公司财务数据库需求分析文档

## 1. 项目背景

投资者在进行上市公司研究时，需要长期跟踪公司财务数据，并将结构化数据提供给 AI 进行分析。直接让大模型从 PDF 年报、季报中读取财务数据存在较高风险，包括识别错误、表格结构丢失、单位误判、报告期误判和字段映射错误。

因此，本项目不以 PDF 解析作为可信数据主路径，而是优先使用结构化数据源，并通过验证系统、版本追溯和人工复核机制提升数据可靠性。

## 2. 项目目标

构建一个本地化、免费优先、可审计、高准确度的上市公司财务数据库。

核心目标：

- 本地保存跟踪公司的三大财务报表数据。
- 支持 CLI 和本地 Web UI 两种操作方式。
- 支持长期跟踪、重复刷新和历史版本保留。
- 检测数据缺失、滞后、异常和不一致。
- 为 AI 分析输出带有来源、时间、新鲜度和可信状态的结构化数据。

## 3. 目标用户

主要用户是个人投资者或小型投研团队。

用户需求包括：

- 关注一组上市公司。
- 定期更新财务数据。
- 减少手工整理财报的工作量。
- 让 AI 使用可追溯、可过滤、可验证的数据。
- 在免费工具和高准确度之间取得平衡。

## 4. 数据范围

第一版覆盖三大财务报表：

- 利润表。
- 资产负债表。
- 现金流量表。

第一版支持市场：

- 美股：`US`。
- A股：`CN`。
- 港股：`HK`。

第一版暂不支持：

- 实时行情。
- 估值指标。
- 投资建议。
- 自动交易。
- 新闻情绪。
- 公告全文分析。
- 自动 PDF 财务表抽取进入可信表。

## 5. 数据源需求

### 5.1 美股数据源

美股优先使用 SEC EDGAR / XBRL / CompanyFacts。

要求：

- 数据来源必须记录。
- 原始响应必须保存。
- 数据应保留报告期、币种、字段名、来源 URL 和抓取时间。
- SEC 结构化数据可标记为最高来源可信度 `official`。

### 5.2 A股和港股数据源

A股和港股优先使用 AKShare 的结构化财务报表接口。

要求：

- 数据来源必须记录为结构化开源来源。
- 原始响应必须保存。
- 若 AKShare 没有最新数据，不允许自动从 PDF 中猜测财务数值。
- 缺失最新结构化数据时，应记录为 `pending_structured_data` 或 `stale`。

### 5.3 爬虫补充层

Python 爬虫可作为补充采集层，但不能替代结构化主数据源。

允许采集：

- 公告标题。
- 披露日期。
- 公告类型。
- PDF/HTML 链接。
- 文件 hash。
- 页面或文件采集时间。

不允许：

- 把未经复核的 PDF/HTML 解析数字直接写入 `financial_facts`。
- 绕过登录、验证码、反爬、付费墙或访问控制。
- 高频访问或并发轰炸目标站点。

爬虫必须遵守 robots.txt、网站条款、限速、缓存和重试上限。

### 5.4 不支持市场

对于暂不支持的市场，系统必须：

- 明确记录为 `unsupported_market`。
- 不静默跳过。
- 不使用低可信度来源自动补数。

## 6. 核心功能需求

### 6.1 数据库初始化

系统应提供初始化命令：

```bash
python main.py init-db --db financial_data.sqlite3
```

初始化应创建所有必要数据表、索引和默认验证规则。重复执行初始化命令不得破坏已有数据。

### 6.2 跟踪清单管理

系统应支持通过 `watchlist.csv` 管理跟踪公司。

字段：

- `symbol`
- `market`
- `name`
- `enabled`
- `notes`

### 6.3 数据刷新

系统应提供刷新命令：

```bash
python main.py refresh --watchlist watchlist.csv --db financial_data.sqlite3
```

刷新流程：

- 读取启用的公司。
- 根据市场选择数据源。
- 获取结构化财务数据。
- 保存原始 payload。
- 标准化财务事实。
- 写入数据库。
- 更新数据新鲜度状态。
- 记录错误和质量问题。
- 自动运行验证。

单家公司刷新失败不应中断整个批处理。

### 6.4 数据验证

系统应提供验证命令：

```bash
python main.py validate --db financial_data.sqlite3
```

验证系统必须支持：

- 来源验证。
- 新鲜度验证。
- 完整性验证。
- 财务关系一致性验证。
- 时间序列异常验证。
- 版本变化验证。

验证结果应写入数据库，而不是只输出到终端。

### 6.5 爬虫补充采集

系统应提供披露补充采集命令：

```bash
python main.py crawl-disclosures --watchlist watchlist.csv --db financial_data.sqlite3
```

爬虫补充层用于判断结构化数据是否滞后，并保存公告元数据。未复核候选数据只能进入 `extracted_candidates` 或 `manual_reviews`。

### 6.6 数据查询和导出

系统应提供导出命令：

```bash
python main.py export --db financial_data.sqlite3 --symbol AAPL --format csv,jsonl --out out/
```

导出数据必须包含：

- 财务事实。
- 来源信息。
- 抓取时间。
- 数据新鲜度状态。
- 验证状态。
- 数据质量评分。

AI 分析默认只应使用 `trusted` 和 `usable` 数据。

### 6.7 本地 Web UI

系统应提供本地 Web 服务：

```bash
python main.py serve --db financial_data.sqlite3 --host 127.0.0.1 --port 8000
```

Web UI 首页为数据质量看板。

页面包括：

- Dashboard：数据质量看板。
- Companies：公司列表。
- Company Detail：公司详情和三大报表。
- Validation Issues：验证问题。
- Runs：刷新和爬虫任务历史。
- Export：导出页面。

Web UI 应支持查看质量状态、刷新历史、数据新鲜度，并允许手动触发刷新和验证。

## 7. 数据库需求

数据库使用 SQLite。SQLite 是本地唯一可信存储，CSV/JSONL 只是导出格式。

逻辑表包括：

- `companies`
- `fetch_runs`
- `financial_facts`
- `raw_payloads`
- `quality_issues`
- `disclosure_events`
- `data_freshness`
- `validation_rules`
- `validation_results`
- `data_quality_scores`
- `manual_reviews`
- `crawler_sources`
- `crawler_runs`
- `crawler_documents`
- `extracted_candidates`

核心原则：

- 原始数据和标准化数据都必须保存。
- 历史版本不得覆盖。
- 可通过 payload hash 判断来源数据是否变化。
- 查询默认返回最新版本，但应能追溯旧版本。

## 8. 数据准确度需求

系统必须满足以下准确度原则：

- 不把旧数据静默当成最新数据。
- 不把未经验证的 PDF 抽取结果写入可信财务事实表。
- 不自动修正原始财务事实。
- 对异常值进行标记，而非删除。
- 对缺失值进行显式记录，而非默认填零。
- 每条财务事实必须可追溯到来源。
- 每条导出给 AI 的记录必须带有可信状态。

数据质量状态包括：

- `trusted`
- `usable`
- `needs_review`
- `stale`
- `failed`

## 9. 非功能需求

### 9.1 本地化

系统应能在本地运行，不依赖远程数据库服务。

### 9.2 免费优先

系统优先使用免费数据源和开源工具。

### 9.3 可审计

系统必须保存原始数据、标准化数据、验证结果、刷新日志和爬虫采集记录。

### 9.4 可扩展

后续应能扩展：

- 新市场。
- 新数据源。
- 人工复核流程。
- 指标计算。
- DuckDB 分析层。
- 更完整的 Web UI。

### 9.5 容错

单个公司、单个市场或单个接口失败，不应导致整个刷新任务失败。

## 10. 验收标准

项目第一版完成时，应满足：

- CLI 可初始化数据库、刷新、验证、导出和启动 Web UI。
- Web UI 可显示数据质量看板。
- SQLite 数据库包含所有核心表。
- 能刷新至少一个 US 公司数据。
- 能刷新至少一个 CN 或 HK 公司数据。
- 能在 AKShare 缺失最新数据时标记为滞后。
- 能保留历史抓取版本。
- 能运行验证规则并保存结果。
- 能导出带可信状态的 CSV/JSONL。
- 能运行合规爬虫补充层，但不污染可信财务事实表。
- 出错时写入日志和质量问题表，批处理不中断。

